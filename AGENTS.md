# TT Realty — Rails App

This is a Rails 8 + Hotwire + Tailwind application.

## Development

```bash
bin/setup
bin/rails db:seed
bin/dev
```

Preview runs on port 3000 by default.

## Demo logins

- Admin: `admin@estate.realty` / `password123`
- Buyer: `buyer@estate.realty` / `password123`
